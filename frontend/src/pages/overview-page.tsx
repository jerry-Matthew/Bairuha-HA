import { OverviewPanel } from '@/components/panels/overview/overview-panel';

/**
 * Overview Page
 * 
 * Displays all entities in the system automatically.
 * This is the main dashboard view that shows real-time entity states.
 */
export function OverviewPage() {
    return <OverviewPanel />;
}
